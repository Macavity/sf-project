<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Ulala Rotation Tool</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% for navItem in navItems %}
        <li class="nav-item">
          <a href="{{ navItem.url }}"
             class="nav-link {% if app.request.attributes.get('_route') == navItem.route %}active{% endif %}"
          >
            {{ navItem.label }}
          </a>
        </li>
        {% endfor %}
      </ul>
{#      <form class="d-flex disabled">#}
{#        <input class="form-control me-2 disabled" disabled type="search" placeholder="Search"#}
{#               aria-label="Search" />#}
{#        <button class="btn btn-outline-success" type="submit" disabled>Search</button>#}
{#      </form>#}
    </div>
  </div>
</nav>
